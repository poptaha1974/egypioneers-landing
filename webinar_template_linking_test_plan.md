# خطة الربط والاختبار الآمن لقوالب ويبنار Egy‑Pioneers

## حدود هذه المرحلة

هذه خطة تنفيذ وليست تشغيلًا. **لن يُنشأ Trigger، ولن يُفعَّل Workflow، ولن تُرسل رسالة إلى أي Lead** في هذه المرحلة. قناة الرسائل الوحيدة عند التنفيذ لاحقاً هي رقم الأكاديمية المصري المتصل داخل FunnelFast: `01025073479`.

تخيّل النظام كأنه بوابة قاعة فيلا 139: **الموافقة هي التذكرة، وسجل الرسائل هو كشف الحضور، والقالب المعتمد هو نص النداء من الميكروفون**. لو التذكرة ناقصة أو الاسم اتنادى قبل كده، البوابة تقفل تلقائياً مهما كان زر الإرسال متاحاً.

## الحالة الحالية التي نبني عليها

| العنصر | الحالة | أثره على الربط |
|---|---|---|
| `egy_webinar_welcome_ar` | Active | جاهز تقنياً للاستخدام لاحقاً، لكنه غير مربوط بأي إرسال. |
| `egy_webinar_reminder_24h_ar` | Pending | يمنع تشغيل تذكير الثلاثاء حتى تتحول حالته إلى Active. |
| `egy_webinar_reminder_3h_ar` | Pending | يمنع تشغيل تذكير الأربعاء حتى تتحول حالته إلى Active. |
| مسودة n8n | غير منشورة | فيها بوابة موافقة وفرز وناتج `queued` فقط، ولا مزود إرسال أو جدولة. |
| WhatsApp Flows في FunnelFast | 0 Flows | لا يوجد Flow جديد أو مسار حي مرتبط بالقوالب. |

> لا يعني وضع **Active** أن رسالة وصلت لأي شخص؛ هو اعتماد القالب من Meta فقط. الإرسال لن يكون ممكناً في التصميم المقترح إلا بعد اجتياز بوابة الموافقة والسجل والتوقيت.

## ما هو موجود بالفعل في قاعدة بيانات الأكاديمية

| الأصل | الحقول الحاسمة | وظيفته في الحماية |
|---|---|---|
| `leads` | `whatsappConsent` و`whatsappConsentAt` | يثبت أن العميل وافق صراحة وتوقيت موافقته. |
| `webinarMessageLogs` | `leadId` و`messageType` و`webinarStartAt` و`status` | يسجل محاولة الرسالة وحالتها. |
| مفتاح فريد | `leadId + messageType + webinarStartAt` | يمنع نفس الرسالة لنفس الويبنار، حتى إذا حدثت إعادة محاولة تقنية. |
| `canQueueWebinarMessage` | موافقة = 1 وغياب سجل سابق | بوابة منطقية موجودة لا تسمح بالاصطفاف بلا إذن أو مع تكرار. |

## مسارا التنفيذ الممكنان

| المسار | كيف يعمل | الميزة | التنازل | قرار الخطة |
|---|---|---|---|---|
| **الحارس المركزي — موصى به** | موقع الأكاديمية يفحص الموافقة والسجل أولاً، ثم يسلّم طلباً واحداً موقّعاً إلى n8n، وn8n يطلب من FunnelFast استخدام القالب الصحيح | أثر تدقيق واضح، ومنع تكرار حقيقي حتى مع إعادة المحاولة، ومرونة في توقيت القاهرة | يحتاج Endpoint داخلي صغير وتعريفاً واضحاً لحالة نجاح وفشل التسليم | **نعتمده عند موافقة التنفيذ** |
| المسار الخفيف داخل FunnelFast فقط | FunnelFast يدير كل منطق الوقت والقالب وDND بنفسه | إعداد أسرع | سجل المنع يصبح موزعاً بين CRM والأكاديمية، ما يقلل وضوح قياس «من اتبعت له إيه ومتى» | لا نستخدمه في الإطلاق الأول |

القرار الموصى به هو **الحارس المركزي**؛ لأنه يحمي رقم الأكاديمية من التكرار كأن أمين الجروب لا يضيف نفس الشخص مرتين، ويعطي الدكتور لوحة مراقبة واحدة للـLead، والرسالة، وحالة الوصول.

## مخطط الربط المقترح عند الموافقة المنفصلة

```text
نموذج صفحة الأكاديمية
  ↓  (حفظ Lead + موافقة WhatsApp)
بوابة queueWebinarMessage داخل الأكاديمية
  ├─ هل whatsappConsent = 1؟
  ├─ هل DND / إلغاء الاشتراك غير مفعل؟
  ├─ هل القالب Active؟
  └─ هل المفتاح الفريد غير موجود؟
       ↓ نعم فقط
webinarMessageLogs = queued
       ↓
n8n Draft → FunnelFast WhatsApp template
       ↓
تحديث السجل: sent / failed / skipped + providerMessageId
```

### القرار الذكي لكل طلب

| الشرط | القرار | ما يسجل في قاعدة البيانات | هل يصل إلى FunnelFast؟ |
|---|---|---|---|
| موافقة صريحة، قالب Active، لا سجل سابق، لا DND | `queued` | سجل جديد فريد | نعم، عند تشغيل الإرسال لاحقاً |
| لا توجد موافقة صريحة | `skipped` | سبب `missing_consent` | لا |
| طلب إلغاء أو DND | `skipped` | سبب `opt_out_or_dnd` | لا |
| سجل للرسالة نفسها والويبنار نفسه | `skipped` | سبب `duplicate_prevented` | لا |
| القالب ما زال Pending | `skipped` | سبب `template_not_active` | لا |
| رفض/خطأ من المزود | `failed` | رسالة الخطأ والوقت | لا يعاد تلقائياً بلا سياسة مراجعة |

## التوقيت الصحيح لويبنار القاهرة

رسالة الترحيب **حدثية**: تُصف عند نجاح التسجيل، وليس عبر جدول. أما التذكيران فهما **دوريان**: الثلاثاء 6:00 مساءً، والأربعاء 3:00 مساءً بتوقيت `Africa/Cairo`.

لن نعتمد ساعة الخادم أو `setInterval`؛ لأن فروق التوقيت الصيفي قد تحول رسالة «قبل 3 ساعات» إلى «بعد الويبنار». سيكون التنفيذ عند الموافقة عبر مهمة دورية تتفحص الساعة المحلية للقاهرة، وتحسب الويبنار التالي، ثم تستدعي نفس بوابة `queueWebinarMessage`. بهذا لا يتغير منطق الحماية بين الترحيب والتذكيرات، وتبقى إعادة تشغيل المهمة آمنة لأن المفتاح الفريد في السجل يمنع التكرار. [1]

## بروتوكول الاختبار الداخلي المقفول

| الخطوة | من يشارك | ماذا نختبر؟ | نتيجة النجاح | شرط الإيقاف |
|---|---|---|---|---|
| 1. اعتماد القوالب | لا أحد من الجمهور | تتحول القوالب الثلاثة إلى Active | ظهور الحالات Active | أي قالب Pending أو Rejected |
| 2. فحص بوابة البيانات | سجل داخلي غير عميل حقيقي | موافقة صريحة، وغياب سجل، وربط الاسم الأول | إنشاء صف `queued` واحد فقط | إنشاء صف مكرر أو تجاوز عدم الموافقة |
| 3. اختبار ترحيب واحد | رقم داخلي مملوك وموافق صراحة | القالب الصحيح، المتغير `first_name`، وتسجيل المعرّف الخارجي | `sent` أو `delivered` مع `providerMessageId` | وصول الرسالة لرقم غير الداخلي أو غياب السجل |
| 4. اختبار منع التكرار | نفس السجل الداخلي | إعادة طلب الرسالة نفسها للويبنار نفسه | `skipped: duplicate_prevented` ولا رسالة ثانية | إرسال مكرر واحد فقط |
| 5. اختبار الإلغاء | رقم الاختبار نفسه | DND / «إلغاء» يمنع المحاولات اللاحقة | `skipped: opt_out_or_dnd` | أي رسالة بعد الإلغاء |
| 6. اختبار توقيت التذكير | محاكاة وقت داخل بيئة مقفولة | اختيار الثلاثاء أو الأربعاء بدقة في `Africa/Cairo` | نوع الرسالة وموعدها صحيحان | وقت مختلف أو ويبنار خاطئ |

لن تُنشأ بيانات تجريبية بطرق تؤثر في بيانات العملاء. رقم الاختبار يجب أن يكون رقمًا داخلياً منفصلاً ومملوكاً للدكتور أو الفريق، مع موافقة مكتوبة، ويُعلّم كسجل اختبار واضح.

## التفعيل المحدود بعد الاختبارات

التفعيل ليس زر واحد؛ هو «حنفية على مرحلتين». أولاً ترحيب محدود لمن يسجلون حديثاً بعد تاريخ التفعيل، ثم مراقبة السجل والـDND، ثم التذكيران بعد نجاح أول دورة. لا يحق للمسار الانتقال بين المراحل تلقائياً.

| المرحلة | النطاق | شرط الانتقال | زر الإيقاف الفوري |
|---|---|---|---|
| صفر | لا إرسال؛ القوالب فقط | اعتماد القوالب الثلاثة | الحالة الحالية |
| واحد | رقم اختبار داخلي واحد | نجاح الاختبارات الستة | أي فشل أو تكرار |
| اثنان | ترحيب للمسجلين الجدد الموافقين فقط | مراجعة الدكتور لسجل الاختبار | أي شكوى أو DND غير محترم |
| ثلاثة | تذكيرا الثلاثاء والأربعاء للموافقين | مراجعة دورة ويبنار كاملة | أي انحراف في توقيت القاهرة أو تقرير التسليم |

## ما يحتاج موافقة تنفيذ مستقلة لاحقاً

سيحتاج التنفيذ موافقة منفصلة ومحددة على ثلاثة تغييرات فقط: إضافة حقل حالة إلغاء الاشتراك في قاعدة بيانات الأكاديمية، إنشاء Endpoint داخلي موثق لاصطفاف الرسالة وتحديث سجلها، وربط مسودة n8n بـFunnelFast بعد التحقق من طريقة إرسال القالب المعتمدة في الحساب. لا يشمل ذلك أي تعديل في Meta Pixel أو AllHomz أو إعلان قائم.

## ما نُفذ في الدفعة الآمنة الأولى

تمت إضافة `whatsappOptedOutAt` إلى بيانات Lead عبر Migration غير هدّام، وإضافة بوابة خادمية إدارية `webinarMessages.queueForReview`. تفحص البوابة وجود الـLead، والموافقة الصريحة، وعدم إلغاء الاشتراك، وغياب سجل سابق لنفس نوع الرسالة ونفس موعد الويبنار؛ ثم تسجل قرار `queued` أو `skipped` فقط. كما أضيف إجراء إداري لتسجيل الإلغاء.

لا تستدعي هذه البوابة n8n أو FunnelFast أو WhatsApp، ولا تسلم أي محتوى خارج قاعدة بيانات الأكاديمية. يحمي المفتاح الفريد داخل `webinarMessageLogs` من سباق إعادة المحاولة بحيث يتحول أي طلب مكرر إلى `duplicate_prevented` بدلاً من رسالة ثانية. اجتازت الاختبارات **43/43** بعد التنفيذ.

## عقد مسودة n8n بعد الربط

أُضيف عقد بيانات خادمي يحوّل قرار `queued` فقط إلى حمولة موجهة لمسار Webhook الخاص بالمسودة `egy-pioneers-webinar-welcome`. تحتوي الحمولة على `leadId` و`messageLogId` والاسم والرقم ونوع الرسالة وموعد الويبنار، مع `whatsappConsent: true` ووسمين ثابتين: `queued_for_review_no_send` و`requiresServerLogCheck`.

قرارات `skipped` لا تحصل على حمولة إطلاقاً، وبالتالي لا يمكنها الوصول إلى مسودة n8n. الأهم: العقد الحالي **لا ينفذ طلب HTTP ولا يستدعي n8n**؛ هو مجرد سلك بيانات معزول يرجع في مخرجات الإجراء الإداري للمراجعة. اجتازت الاختبارات **45/45**، ومنها اختبار يثبت أن غياب الموافقة لا ينشئ أي حمولة للمسودة.

## المراجع

[1]: https://developers.facebook.com/documentation/business-messaging/whatsapp/webhooks/overview "Meta — WhatsApp Business Platform Webhooks"
[2]: https://developers.facebook.com/documentation/business-messaging/whatsapp/getting-opt-in "Meta — WhatsApp opt-in requirements"
[3]: https://help.gohighlevel.com/support/solutions/articles/155000001624-whatsapp-workflow-integration "HighLevel — WhatsApp workflow integration"
