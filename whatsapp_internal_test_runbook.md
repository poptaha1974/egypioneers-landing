# معاينة اختبار واتساب الداخلي المقفول — Egy‑Pioneers

## نطاق الاختبار

هذا الاختبار مخصص لرقم شخصي مملوك للدكتور إيهاب وقد وصل في المحادثة فقط. **لا يُحفظ الرقم في الكود أو الوثائق أو قاعدة بيانات الإنتاج قبل تأكيد الإرسال النهائي.** لا توجد قائمة جمهور، ولا Trigger، ولا Workflow نشط، ولا تذكيرات في هذه التجربة.

| البند | القيمة المقفلة |
|---|---|
| رقم الإرسال | رقم الأكاديمية المصري المتصل `01025073479` |
| المستلم | رقم الدكتور الشخصي الذي تم تقديمه في المحادثة؛ استخدام لمرة اختبار واحدة فقط |
| القالب | `egy_webinar_welcome_ar` — Arabic / Utility / Active |
| متغير الاسم | `إيهاب` |
| عدد الرسائل المسموح | رسالة واحدة فقط |
| n8n | غير منشور ولا يُستدعى |
| FunnelFast Flows | لا يوجد Flow جديد أو عام |

## معاينة الرسالة قبل الإرسال

> أهلاً يا إيهاب، تم تسجيلك في الجزء المفتوح من ويبنار Egy-Pioneers الأسبوعي يوم الأربعاء من 6 إلى 9 مساءً بتوقيت القاهرة. أول 30 دقيقة مفتوحة، وبعدها تستكمل الورشة لأعضاء نادي تجار العرب. قبل الموعد بساعة هنرسل لك تفاصيل الدخول هنا. لو لم تطلب التسجيل أو لا تريد متابعة الرسائل، اكتب لنا: إلغاء.

## حاجز الإرسال الإجباري

لن تُنشأ جهة اتصال اختبارية ولن تُرسل الرسالة ما لم يصل تأكيد منفصل وصريح بعبارة تعني: **«أرسل اختبار الترحيب الآن إلى رقمي الشخصي»**. عندها فقط يتم تنفيذ إرسال يدوي واحد للقالب داخل FunnelFast، ثم تُراجع النتيجة دون تشغيل أي أتمتة.

| بعد الإرسال | المطلوب توثيقه | معيار النجاح |
|---|---|---|
| خلال دقائق | وقت الإرسال، اسم القالب، وحالة FunnelFast | ظهور Delivered أو حالة مزود واضحة |
| عند وصول الرسالة | مطابقة الاسم والنص والرقم | وصول رسالة واحدة فقط إلى رقم الدكتور |
| بعد التجربة | تسجيل أن الاختبار انتهى | لا إعادة تلقائية ولا تذكير ولا جمهور عام |

إذا لم تصل الرسالة أو ظهرت حالة خطأ، يتوقف المسار عند التشخيص؛ لا توجد إعادة محاولة تلقائية ولا انتقال إلى أي رقم أو Lead آخر.

## نتيجة التنفيذ اليدوي

في 16 أغسطس 2026، تم إرسال **رسالة واحدة** بالقالب `egy_webinar_welcome_ar` يدوياً من رقم الأكاديمية `+20 10 25073479` إلى رقم الاختبار الشخصي المقدم في المحادثة. تم تصحيح الاسم الأول لجهة الاختبار إلى `إيهاب` قبل الإرسال، وظهر في نص المحادثة بشكل صحيح: «أهلاً يا إيهاب».

لم يُفعّل n8n، ولم يُنشأ Trigger أو FunnelFast Flow أو جمهور عام، ولم تُرسل رسالة ثانية. واجهة المحادثة أثبتت إدراج الرسالة، لكنها لم تعرض `providerMessageId` أو حالة `delivered` قابلة للقراءة؛ لذلك لا يُعتبر تدقيق التسليم مكتملاً حتى يؤكد الدكتور وصولها على هاتفه أو تظهر حالة مزود واضحة. كما أن هذا الإرسال اليدوي لا يمر بعد بسجل `webinarMessageLogs`، وهو سبب إضافي يمنع أي تشغيل عام قبل إتمام ربط السجل لاحقاً.

## دليل الوصول الحي

قدم الدكتور إيهاب لقطة حية من هاتفه بعد الاختبار. تثبت اللقطة وصول رسالة واتساب من محادثة الأكاديمية، وتُظهر بداية التحية الصحيحة: **«أهلاً يا إيهاب»**، مع كامل نص الويبنار المعتمد ووقت الرسالة `02:54`. هذا دليل استلام من الجهاز المستلم، أقوى من حالة واجهة FunnelFast وحدها في هذا الاختبار.

تمت مطابقة عناصر الاختبار التالية: رقم الإرسال هو رقم الأكاديمية المصري الذي اختير قبل الإرسال، المستلم هو رقم الاختبار الشخصي المتفق عليه، القالب هو `egy_webinar_welcome_ar`، ومتغير الاسم ظهر صحيحاً. لم يظهر في السجل أو اللقطة أي تذكير أو رسالة ثانية أو جمهور آخر. لذلك نجح الاختبار الفردي، لكن يظل الإرسال العام متوقفاً حتى يُربط الإرسال بسجل `webinarMessageLogs` وبوابتي الموافقة وDND قبل أي Trigger أو جدولة.
