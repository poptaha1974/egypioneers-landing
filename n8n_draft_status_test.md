# اختبار وسم حالة المسودة

بعد ضبط الحقل `draftLogStatus` على `draft_received_no_send`، شُغّل وضع Test Webhook داخل المسودة غير المنشورة. الواجهة تؤكد أنها تنتظر رابط الاختبار فقط، وزر `Publish` ما زال ظاهراً. لا توجد رسالة واتساب أو Trigger دائم أو Schedule في هذه الخطوة.
