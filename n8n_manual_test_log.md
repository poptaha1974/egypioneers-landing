# سجل اختبار Webhook اليدوي لمسودة n8n

## محاولة الفتح

بتاريخ 16 أغسطس 2026 فُتح رابط مسودة الويبنار `XFseuAOOMLzKZZ5D` في المتصفح المتصل. لم تُظهر واجهة المتصفح عناصر المحرر أو لقطة قابلة للتفاعل رغم عودة عنوان n8n الصحيح. لم يُضغط `Publish`، ولم يُشغّل Test Webhook، ولم يتغير أي إعداد في المسودة.

## الحالة

يتطلب الاختبار التالي أن يُشغَّل من داخل محرر n8n المرئي فقط: فتح عقدة `Webhook`، الضغط على `Listen for test event`، ثم إرسال POST واحد إلى رابط الاختبار ومراجعة توقف المسار عند `Respond to Webhook`. لن يتضمن ذلك مزود إرسال أو إعداد Schedule أو أي رسالة واتساب.
