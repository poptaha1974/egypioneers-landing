# مسودة رسالة الترحيب وWorkflow الويبنار

## النص المقترح للمسجل

> أهلاً يا {{first_name}}، تم تسجيلك في الجزء المفتوح من ويبنار Egy‑Pioneers الأسبوعي يوم الأربعاء من 6 إلى 9 مساءً بتوقيت القاهرة. أول 30 دقيقة مفتوحة، وبعدها تستكمل الورشة لأعضاء نادي تجار العرب. قبل الموعد بساعة هنرسل لك تفاصيل الدخول هنا. لو لم تطلب التسجيل أو لا تريد متابعة الرسائل، اكتب لنا: إلغاء.

النص يثبت التسجيل ويشرح الخطوة التالية من غير وعد بنتيجة أو ضغط نفسي أو ادعاء قرار مصيري.

## مسار الـWorkflow المقترح

| الترتيب | العقدة | الهدف | شرط النجاح |
|---|---|---|---|
| 1 | Trigger من تسجيل Lead ناجح | بدء المسار بعد حفظ قاعدة البيانات | وجود lead_id ورقم واتساب مصري صالح |
| 2 | فلتر موافقة التواصل | منع أي رسالة دون موافقة واضحة من الزائر | whatsapp_consent = true |
| 3 | Rate limit | منع تكرار الترحيب لنفس الرقم | لا توجد رسالة ترحيب خلال 7 أيام |
| 4 | Send WhatsApp template | إرسال النص المقترح من رقم الأكاديمية | استجابة نجاح من مزود واتساب |
| 5 | سجل الرسالة | حفظ حالة الإرسال ووقته وسبب الفشل إن وجد | message_status = sent أو failed |
| 6 | مسار فشل آمن | تنبيه الفريق فقط من دون إعادة إرسال مزعجة | لا تتجاوز محاولة واحدة تلقائياً |

## ضوابط التنفيذ

لا ينشأ الـWorkflow ولا يُفعّل قبل إضافة موافقة واضحة في النموذج واعتماد هذا النص. لا يستخدم أي Pixel آخر غير Pixel الأكاديمية، ولا يغير CAPI أو Workflow التصنيف الحالي. يظل فتح واتساب اليدوي الحالي بديلاً مرئياً إذا تعذر الإرسال التلقائي.

## نصوص التذكير المقترحة

### قبل الويبنار بـ24 ساعة

> أهلاً يا {{first_name}}، فاضل يوم على ويبنار Egy‑Pioneers غداً الأربعاء من 6 إلى 9 مساءً بتوقيت القاهرة. أول 30 دقيقة مفتوحة، وبعدها تستكمل الورشة لأعضاء نادي تجار العرب. احفظ الموعد، ورابط الدخول هيوصلك هنا قبل البداية بساعة. لو لا تريد متابعة الرسائل، اكتب لنا: إلغاء.

### قبل الويبنار بـ3 ساعات

> مساء الخير يا {{first_name}}، فاضل 3 ساعات على ويبنار Egy‑Pioneers اليوم الساعة 6 مساءً بتوقيت القاهرة. جهّز مكان هادئ ودفتر ملاحظات، ورابط الدخول هيوصلك هنا قبل الموعد بساعة. لو ظروفك اتغيرت، اكتب لنا: تأجيل أو إلغاء.

## تصميم الجدولة الآمن

يعمل مشروع Heartbeat كل 15 دقيقة، ثم يحسب التوقيت محلياً بتوقيت القاهرة بدلاً من تثبيت ساعة UTC قد تتغير مع التوقيت الصيفي. يرسل فقط عند توافق الوقت مع الثلاثاء 6 مساءً أو الأربعاء 3 مساءً، مع سجل حالة منفصل لكل Lead ولكل نوع رسالة. بهذه الطريقة لا يتلقى الشخص نفسه الرسالة مرتين عند إعادة المحاولة، ولا ترسل الرسائل لمن ألغى الموافقة أو لم يوافق عليها أساساً.

الـWorkflow المقترح يبقى مستقلاً عن `Egy Pioneers - Lead Qualification & Sales Alert`؛ فهو لا يغير تصنيف HOT/WARM/COLD ولا يلمس Events CAPI. وظيفته الوحيدة هي إدارة الاتصال الخدمي للمسجلين الموافقين.

## حالة n8n قبل الإنشاء

تحتوي مساحة n8n الحالية على Workflow منشور لتصنيف Leads الأكاديمية، وWorkflow منشور لإشارة `messaging_first_reply`، وWorkflow غير منشور باسم `My workflow`. مسودة الويبنار الجديدة ستنشأ باسم واضح ومختلف: `DRAFT - Egy-Pioneers Webinar Welcome & Reminders`، وتبقى غير مفعّلة ولا تعدّل أي Workflow قائم.

## حالة المسودة بعد الإنشاء

تم إنشاء المسودة بالاسم المعتمد `DRAFT - Egy-Pioneers Webinar Welcome & Reminders` وهي ما زالت غير منشورة وغير مفعّلة. يبدأ المسار بـWebhook من نوع `POST` على المسار `egy-pioneers-webinar-welcome`، ثم فلتر `If` يمرّر فقط السجلات التي تحمل `whatsappConsent = true`، ثم عقدة تجهيز بيانات تحتفظ ببيانات الإدخال وتضيف الحالة `queued_for_review_no_send`.

لا تحتوي المسودة على مزود إرسال واتساب، أو بيانات اعتماد لمزود، أو عقدة إرسال، أو جدول فعّال للتذكيرات. وسيظل ربط سجل منع التكرار، وفرز أنواع الرسائل الثلاثة، وجدولة الثلاثاء والأربعاء، ورد الـWebhook الموحّد، معلقاً للمراجعة النهائية واعتماد مزود إرسال قانوني قبل التفعيل.

## استكمال الدفعة الآمنة داخل المسودة

أُضيفت عقدة `Route message type` لتصنيف `welcome` و`reminder_24h` و`reminder_3h`، وعقدة `Deduplication pending` التي تضيف العلامة `requires_server_log_check` مع الاحتفاظ ببيانات الإدخال. هذه العلامة توثيقية فقط؛ لا تستعلم عن قاعدة بيانات ولا تستخدم بيانات اعتماد أو طلب HTTP خارجي، إلى أن يراجع المستخدم ربط سجل `webinarMessageLogs` الفعلي.

كما أُضيفت عقدة `Respond to Webhook` واستجابة JSON الآتية: `queued: true` و`messageType` من الطلب و`delivery: not_configured`. إعداد Webhook مضبوط على استخدام عقدة الرد هذه. لذلك، حتى عند اكتمال الربط لاحقاً، يظل هذا الجزء مجرد تأكيد استلام ولا يرسل واتساب.

تمت مراجعة المسودة بصرياً: لا توجد عقدة مزود واتساب، ولا بيانات اعتماد، ولا عقدة إرسال، ولا `Schedule Trigger`، وزر `Publish` ما زال ظاهراً، بما يثبت أن الـWorkflow غير منشور. جرى اختبار داخلي تلقائي واحد بواسطة مساعد n8n على بيانات محاكاة، فتوقف عند فلتر الموافقة قبل وصوله للعقد الجديدة؛ تم إيقافه فوراً، ولم يصدر عنه إرسال أو اتصال خارجي. لم يُنفذ أي اختبار جديد بعد ذلك.
