# تحديث حالة سجل رسائل الويبنار بعد استلام المسودة

## خريطة الحالات

| الحالة | المعنى | هل خرجت رسالة واتساب؟ |
|---|---|---|
| `queued` | التسجيل مؤهل ودخل طابور المراجعة | لا |
| `skipped` | توقّف بسبب غياب الموافقة أو الإلغاء أو التكرار | لا |
| `draft_received` | مسودة n8n ردت صراحةً بأنها استلمت `queued` | لا |
| `sent` / `failed` | محجوزتان فقط لمرحلة مزود الإرسال المستقبلية | لا تستخدمان الآن |

أضيف تحديث قاعدة البيانات من `queued` إلى `draft_received` فقط إذا أعاد Webhook المسودة استجابة ناجحة تتضمن `queued: true` و`delivery: not_configured`. أي فشل HTTP أو استجابة غير صريحة أو قرار `skipped` يترك السجل كما هو ولا يرسل أي طلب جديد.

داخل n8n أضيف وسم مخرجات `draftLogStatus: draft_received_no_send` في عقدة الحالة قبل `Respond to Webhook`. اختبر المسار يدوياً بنجاح، وبقي زر `Publish` ظاهراً؛ لا توجد عقدة مزود واتساب أو Trigger دائم أو Schedule.
